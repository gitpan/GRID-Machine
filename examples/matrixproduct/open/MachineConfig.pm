package main;
our %debug = (europa => 0, beowulf => 0, orion => 0, bw => 0, #gorgorito => 0
            );
our %max_num_proc = (europa => 1, beowulf => 1, orion => 1, bw => 1, #gorgorito => 1
                   );

1;
